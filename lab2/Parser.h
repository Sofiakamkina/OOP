#pragma once

#include <string>
#include <algorithm>
#include <vector>
#include <stack>
#include <map>
#include "consts.h"
#include "Operator.h"
#include <vector>
#include <string>
using std::string;
using std:: map;
class Parser
{
private:
public:
    std::vector<string> parser(string str);
};
